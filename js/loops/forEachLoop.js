// forEach loop in js

// array.forEach( () => {} )
// callback function = (() => {})

let studentName = ["bhanwar lal", "lokesh", "roshan"];

studentName.forEach((student) => {
    console.log(student)
})